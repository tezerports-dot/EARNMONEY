/**
 * Web build: there is no mobile ads SDK in a browser, so this does nothing.
 * Web advertising is a separate product (AdSense) and a separate decision.
 */
export function initialiseAds(): void {}
